@using Diabetes.MVC.Models.Components
@await Component.InvokeAsync("AddSelector", new{ model = new AddSelectorModel(true, false, false) })

@{
    ViewData["Title"] = "Приём пищи";
}

Тут будет добавление еды